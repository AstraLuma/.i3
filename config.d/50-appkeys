# start a terminal
bindsym $mod+Return exec lilyterm

# Different menus
# Simple listing of path (plain dmenu)
bindsym $mod+Shift+d exec dmenu_run
# "User friendly" listing of desktop files
bindsym $mod+d exec --no-startup-id j4-dmenu-desktop

# Python console, because I use it enough
bindsym $mod+p exec gnome-terminal -e python3

# Console to a common directory
bindsym $mod+t exec ~/.i3/zmensh

# Calculator
bindsym $mod+c exec galculator
